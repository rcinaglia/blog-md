node_modules
.output
.git
.gitignore
Dockerfile
docker-compose.yaml
.dockerignore

# Content is bind-mounted at runtime (see docker-compose.yaml), no need
# to ship it inside the image.
content

# Env files are bind-mounted at runtime, not baked into the image.
.env
.env.local
*.local

.vscode
README.md
AGENTS.md
